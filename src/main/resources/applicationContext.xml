<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"

       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- XML beans -->
    
    <!-- включает аннотации -->
    <context:annotation-config/>

    <!-- сканирует компоненты -->


    <bean id="starter" class="lesson_N4.XML.Starter"/>
    <bean id="sparkPlug" class="lesson_N4.XML.SparkPlug"/>
    <bean id="hinge" class="lesson_N4.XML.Hinge"/>
    <bean id="differential" class="lesson_N4.XML.Differential"/>
    <bean id="accumulator" class="lesson_N4.XML.Accumulator"/>
    <bean id="wheel" class="lesson_N4.XML.Wheel"/>

    <bean id="engine" class="lesson_N4.XML.Engine">
        <constructor-arg ref="starter"/>
        <constructor-arg ref="sparkPlug"/>
    </bean>
    <bean id="suspension" class="lesson_N4.XML.Suspension">
        <constructor-arg ref="hinge"/>
        <constructor-arg ref="differential"/>
    </bean>

    <bean id="car" class="lesson_N4.XML.Car">
        <constructor-arg ref="engine"/>
        <constructor-arg ref="suspension"/>
        <constructor-arg ref="accumulator"/>
        <constructor-arg ref="wheel"/>

    </bean>



   


</beans>